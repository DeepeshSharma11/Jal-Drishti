# Jal Drishti Database

Run these SQL files in Supabase SQL Editor in order:

1. `schema.sql`
2. `seed.sql`

`schema.sql` creates the required tables, indexes, RLS policies, and `hotspot-images` storage bucket.

`seed.sql` inserts the official hotspot dataset into `verified_hotspot`.
